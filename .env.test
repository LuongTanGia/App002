NODE_ENV=test
JWT_SECRET=test_jwt_secret_key_for_testing_purposes_only_very_long_and_secure
MONGO_URI=mongodb://localhost:27017/app002_test
PORT=3001

# Rate limiting for tests (more lenient)
RATE_LIMIT_GLOBAL_MAX=10000
RATE_LIMIT_GLOBAL_WINDOW=900000
RATE_LIMIT_AUTH_MAX=100
RATE_LIMIT_AUTH_WINDOW=900000

# Performance settings for tests
REQUEST_TIMEOUT=10000
MAX_REQUEST_SIZE=1mb

# Cache settings for tests
CACHE_TTL=60

# Database settings for tests
DB_MAX_POOL_SIZE=5
DB_MIN_POOL_SIZE=2
DB_MAX_IDLE_TIME_MS=10000
DB_SERVER_SELECTION_TIMEOUT_MS=2000
DB_SOCKET_TIMEOUT_MS=15000
